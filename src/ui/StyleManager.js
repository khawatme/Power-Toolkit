import '../assets/style.css';

/**
 * Manages CSS stylesheet injection via Webpack's style-loader.
 */
export const StyleManager = {
    /**
     * Ensures the module is loaded and styles are injected.
     */
    init() { }
};